name: 'Bootstrap environment'
description: 'Setup Node with npm cache, run npm install'
runs:
  using: composite
  steps:
    - name: Setup Node
      uses: actions/setup-node@v3
      with:
        node-version: '18.0'
        cache: 'npm'
    - name: Install dependencies
      run: npm install
      shell: bash
